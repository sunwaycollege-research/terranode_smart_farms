# TerraNode web admin — Vite environment
# VITE_API_URL is embedded at build time — set to the server's LAN IP
# so the web panel works from any device on the network.

VITE_API_URL=http://10.59.10.197:4000
PORT=5173

# Copy to .env and adjust. The app reads these via app.json -> extra by default;
# for real builds wire these through app.config.ts or EAS secrets.
API_BASE_URL=http://localhost:4000
USE_MOCK_API=false
